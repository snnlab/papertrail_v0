<!-- papertrail:history -->
# Reconstructed History (pre-adoption)

Reconstructed at adoption on <YYYY-MM-DD>; covers <first commit date> – <Initialized>.
<!-- This file is a RECORD, not the decision log. Its entries are reconstructions from git
     evidence, documents, and memory, written after the fact — they carry no real-time
     guarantee and every claim cites its evidence. The decision log begins at the master
     plan's `Initialized:` timestamp and is unaffected by anything here.

     Scope: entries record only events dated BEFORE `Initialized:`. Anything at or after the
     cutoff belongs in decision-log.md (via `/papertrail:sync` if late) — never here.
     You may append to this file at any time (whenever more of the past is recovered), but
     never to log the present. Date-granularity headers only; never fabricate a clock time. -->

## <YYYY-MM or YYYY-MM-DD> — <what happened, in a phrase>

**Evidence:** <commit hashes, doc paths, or "student recollection" — one of these per claim>
**Decision / turn:** <what was decided or what changed course, as remembered>
**Uncertain:** <optional — what cannot be pinned down: a date, a rationale, an ordering>

<!-- Newest last is fine, but ordering is by the event date in the header, not append order.
     The board Timeline renders these as distinct "reconstructed" cards before the cutoff. -->
